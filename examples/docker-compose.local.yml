# Optional loopback port binding for either Linux or macOS local development.
# On macOS, also include docker-compose.macos.yml for the seccomp file path.
services:
  codex-broker:
    ports:
      - "127.0.0.1:3400:3400"
