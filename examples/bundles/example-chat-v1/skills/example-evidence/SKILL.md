---
name: example-evidence
description: Use host-owned evidence tools through the broker adapter.
---

# Example Evidence

Use `host.evidence.search` when product evidence is needed. The host app owns identity, authorization, evidence retrieval, data models, and result semantics; treat the tool response as opaque product data from the host app.
