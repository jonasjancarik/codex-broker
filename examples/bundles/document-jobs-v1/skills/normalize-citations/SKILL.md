---
name: normalize-citations
description: Normalize document citations for host-owned jobs.
---

# Normalize Citations

Use the mounted workspace as the source of job inputs and artifacts. The host app owns job records, queues, review rows, and generated output semantics; the broker owns only managed Codex execution and event streaming.
