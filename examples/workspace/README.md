# Example workspace

This deliberately narrow directory is mounted at `/workspaces/app` by the
example Compose service. Replace it only with the individual job or project
directory that a broker turn may use; never mount a repository parent or home
directory for convenience.
