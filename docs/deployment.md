# Deployment

read_when: building or publishing the image, extending the broker container, changing Docker mounts or secrets, deploying beside a host app, or changing readiness and shutdown behavior.

The canonical reader-facing documentation is
[Fern: Deployment](../fern/docs/pages/operations/deployment.mdx).

This compatibility pointer preserves existing repository links and agent
`read_when` routing. Update the Fern page rather than adding content here.
