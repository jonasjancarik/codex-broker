# Optional loopback port binding for either Linux or macOS local development.
# Set CODEX_BROKER_SECCOMP_PROFILE for a client-local seccomp file path.
services:
  codex-broker:
    ports:
      - "127.0.0.1:3400:3400"
